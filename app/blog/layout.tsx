import "../../pages-source/blog/app/global.css";

export const metadata = {
  title: "Aspire Rwanda: Blogs",
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
