import "../../pages-source/about-ar/app/global.css";

export const metadata = {
  title: "Aspire Rwanda: About Us",
};

export default function AboutArLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
