import "../../pages-source/nursery/app/global.css";

export const metadata = {
  title: "Aspire Rwanda: Nursery",
};

export default function NurseryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
